export interface project {
    id: number,
    name: string,
    members: string[], // así queda de momemto, cuando trabaje con Login voy a tener una interface de usuarios o alguna forma de relacionarlo
    description: string,
    icon: string,
};
