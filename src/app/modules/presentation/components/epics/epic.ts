export interface epic{
    id: number,
    name: string,
    projectId: number, // de momento la dejo así, cuando trabaje con API actualizo el esquema con el código comentado abajo.
    description: string,
    icon: string,
};
